# ============================================
# REQUIRED ENVIRONMENT VARIABLES
# ============================================

# AI - adesso AI Hub (OpenAI-compatible)
# Get your API key from adesso AI Hub
OPENAI_API_KEY=your-api-key-here
OPENAI_BASE_URL=https://adesso-ai-hub.3asabc.de/v1

# Authentication Secret
# Generate with: openssl rand -base64 32
# IMPORTANT: Keep this secret and never commit to Git
AUTH_SECRET=your-generated-secret-here

# Database Connection (PostgreSQL with pgvector)
# Local development: docker compose up -d postgres
DATABASE_URL=postgresql://dealhunter:dealhunter@localhost:5433/dealhunter

# PostgreSQL Docker credentials (override defaults for production)
# These are used by docker-compose.yml
POSTGRES_USER=dealhunter
POSTGRES_PASSWORD=dealhunter
POSTGRES_DB=dealhunter

# Database connection pool size (default: 50)
# Increase for high concurrency scenarios (e.g., parallel deep scan experts)
DB_POOL_SIZE=50

# Redis Connection (for BullMQ background jobs)
# Local development: docker compose up -d redis
REDIS_URL=redis://:dealhunter@localhost:6379
REDIS_PASSWORD=dealhunter

# ============================================
# OPTIONAL ENVIRONMENT VARIABLES
# ============================================

# Anthropic - adesso AI Hub (optional, for Claude models)
# Only needed if using Anthropic models
ANTHROPIC_API_KEY=
ANTHROPIC_BASE_URL=https://adesso-ai-hub.3asabc.de

# Exa API Key (optional, for semantic web search)
# Get your key from: https://exa.ai
EXA_API_KEY=

# OpenAI Direct API Key (for GPT-5 models and embeddings)
# Required for GPT-5 models and document embeddings
# Get your key from: https://platform.openai.com/api-keys
# Note: This uses direct OpenAI API (not adesso AI Hub)
OPENAI_DIRECT_API_KEY=

# OpenAI Embedding API Key (legacy, falls back to OPENAI_DIRECT_API_KEY)
OPENAI_EMBEDDING_API_KEY=

# Gemini API Key (for fast analysis agents)
# Get your key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=

# Slack Bot Integration (optional - leave empty to disable notifications)
# Get tokens from: https://api.slack.com/apps
SLACK_BOT_TOKEN=
SLACK_SIGNING_SECRET=
SLACK_CHANNEL_ID=

# ============================================
# AI MODEL CONFIGURATION (Per-Model Settings)
# ============================================
# Each model slot can be configured independently.
# Provider options: 'ai-hub' or 'openai' (default: openai with GPT-5)
#
# Format: AI_MODEL_<SLOT>_PROVIDER and AI_MODEL_<SLOT>_NAME
#
# DEFAULTS (if not set):
# - fast: openai/gpt-5-nano (fastest, most cost-efficient)
# - default: openai/gpt-5-mini (well-defined tasks like extraction)
# - quality: openai/gpt-5.2 (best for coding and agentic tasks)
# - premium: openai/gpt-5.2-pro (smarter, more precise responses)
# - synthesizer: openai/gpt-5-nano (low latency for section synthesis)
# - research: openai/o4-mini-deep-research (autonomous multi-step research)

# Fast model (for quick operations, low latency)
# AI_MODEL_FAST_PROVIDER=openai
# AI_MODEL_FAST_NAME=gpt-5-nano

# Default model (general purpose, extraction)
# AI_MODEL_DEFAULT_PROVIDER=openai
# AI_MODEL_DEFAULT_NAME=gpt-5-mini

# Quality model (agentic tasks, complex analysis)
# AI_MODEL_QUALITY_PROVIDER=openai
# AI_MODEL_QUALITY_NAME=gpt-5.2

# Premium model (critical decisions, highest quality)
# AI_MODEL_PREMIUM_PROVIDER=openai
# AI_MODEL_PREMIUM_NAME=gpt-5.2-pro

# Synthesizer model (for section synthesis, needs low latency)
# AI_MODEL_SYNTHESIZER_PROVIDER=openai
# AI_MODEL_SYNTHESIZER_NAME=gpt-5-nano

# Research model (for deep company/market research)
# AI_MODEL_RESEARCH_PROVIDER=openai
# AI_MODEL_RESEARCH_NAME=o4-mini-deep-research
# Alternative: o3-deep-research (more powerful, slower)

# ============================================
# ADVANCED CONFIGURATION (Usually not needed)
# ============================================

# Next.js URL (auto-detected in development)
# Only set this in production if using custom domain
# NEXTAUTH_URL=https://your-domain.com

# Checkpoint System Configuration
# Directory for workflow checkpoint files (default: ./tmp/checkpoints)
# CHECKPOINT_DIR=/custom/path/to/checkpoints

# Keep completed checkpoints for debugging (default: false)
# Set to 'true' to rename instead of deleting completed checkpoints
# KEEP_CHECKPOINTS=false

# Checkpoint retention period in days (default: 7)
# Old checkpoints will be automatically cleaned up after this many days
# CHECKPOINT_RETENTION_DAYS=7

# Node Environment (auto-detected)
# NODE_ENV=development

# Development Server Port (default: 3000)
# PORT=3000
